(defun sum-of-multiples(n, m)
  "Return sum of multiples of numbers passed in m up to the number n."
  (setq count 0)
  (loop
   (setq count (+ count 1))
   (if (= (mod i o) 0)
   (if (= count (- n 1)))
   )
  )
)
